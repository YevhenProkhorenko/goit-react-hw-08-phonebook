import React from 'react';
import css from '../HomePage/HomePage.module.scss';

const HomePage = () => (
  <div className={css.wrapper}>
    <h1 className={css.title}>To view contacts, log in to your account</h1>
  </div>
);

export default HomePage;
